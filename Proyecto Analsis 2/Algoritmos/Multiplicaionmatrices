matrices(array[])
        i <- 1
        j <- count(array) - 1
        return Multiplicaciondematrices(array, i, j)
Multiplicaciondematrices(array[],i,j) 
        if (i = j) then
            return 0
        min <- ∞
        for (int k <- i; k < j; k++) do
            count <- Multiplicaciondematrices(array, i, k)+ Multiplicaciondematrices(array, k + 1, j)+ array[i - 1] * array[k] * array[j]
            if (count < min) then
                min <- count
        return min